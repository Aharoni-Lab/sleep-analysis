"""
Metaclass for data models

(will be empty for now, but we put it in when we start
so that it's there when we need it later)
"""

from dataclasses import dataclass
import numpy as np

@dataclass
class DataModel:
    """
    Metaclass for data models
    """




